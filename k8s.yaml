# BE CAREFUL MODIFYING THIS FILE
apiVersion: apps/v1
kind: Deployment
metadata:
  name: APP
spec:
  selector:
    matchLabels:
      app: APP
  replicas: 3
  template:
    metadata:
      labels:
        app: APP
    spec:
      containers:
      - name: APP
        image: gcr.io/ORG/NAME:VERSION
        command: ["./eligibility"]
        args: ["--env=ENV"]
        env:
        - name: ELIGIBILITY_DB_HOST
          valueFrom:
            configMapKeyRef:
              name: DATA
              key: ELIGIBILITY_DB_HOST
        - name: ELIGIBILITY_DB_PORT
          valueFrom:
            configMapKeyRef:
              name: DATA
              key: ELIGIBILITY_DB_PORT
        - name: ELIGIBILITY_DB
          valueFrom:
            configMapKeyRef:
              name: DATA
              key: ELIGIBILITY_DB
        - name: ELIGIBILITY_DB_USER
          valueFrom:
            configMapKeyRef:
              name: DATA
              key: ELIGIBILITY_DB_USER
        - name: ELIGIBILITY_DB_PASSWORD
          valueFrom:
            configMapKeyRef:
              name: DATA
              key: ELIGIBILITY_DB_PASSWORD
        - name: JWT_SECRET
          valueFrom:
            configMapKeyRef:
              name: DATA
              key: JWT_SECRET
        ports: 
        - containerPort: 8184
        resources: {}
      
---
apiVersion: batch/v1
kind: Job
metadata:
  name: MIGRATION
spec:
  ttlSecondsAfterFinished: 100
  template:
    metadata:
      labels:
        app: MIGRATION
    spec:
      containers:
      - name: MIGRATION
        image: gcr.io/ORG/NAME-migration:VERSION
        args: ["composeup"]
        env:
        - name: ELIGIBILITY_DB_HOST
          valueFrom:
            configMapKeyRef:
              name: DATA
              key: ELIGIBILITY_DB_HOST
        - name: ELIGIBILITY_DB_PORT
          valueFrom:
            configMapKeyRef:
              name: DATA
              key: ELIGIBILITY_DB_PORT
        - name: ELIGIBILITY_DB
          valueFrom:
            configMapKeyRef:
              name: DATA
              key: ELIGIBILITY_DB
        - name: ELIGIBILITY_DB_USER
          valueFrom:
            configMapKeyRef:
              name: DATA
              key: ELIGIBILITY_DB_USER
        - name: ELIGIBILITY_DB_PASSWORD
          valueFrom:
            configMapKeyRef:
              name: DATA
              key: ELIGIBILITY_DB_PASSWORD
        - name: JWT_SECRET
          valueFrom:
            configMapKeyRef:
              name: DATA
              key: JWT_SECRET
      restartPolicy: Never
  backoffLimit: 4
---

apiVersion: v1
kind: Service
metadata:
  name: APP
spec:
  selector:
    app: APP
  ports:
  - port: 80
    targetPort: 8184
  type: LoadBalancer
